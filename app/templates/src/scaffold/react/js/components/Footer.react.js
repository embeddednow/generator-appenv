"use strict";

var React = require('react');

var Footer = React.createClass({
  render: function() {
    return (
      <footer>
        <h1>This is the footer</h1>
      </footer>
    );
  }
});

module.exports = Footer;
