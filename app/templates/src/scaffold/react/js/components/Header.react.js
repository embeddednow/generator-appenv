"use strict";

var React = require('react');

var Header = React.createClass({
  render: function() {
    return (
      <header>
        <h1>This is the header</h1>
      </header>
    );
  }
});

module.exports = Header;
